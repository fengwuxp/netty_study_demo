option java_package = "com.wuxp.study.proto";
option java_outer_classname = "ResponeDTO"; //生成的数据访问类的类名

message Respone {
    required int32 id = 1; //同上
    required string name = 2; //必须字段，在后面的使用中必须为该段设置值
    optional string email = 3; //可选字段，在后面的使用中可以自由决定是否为该字段设置值
    required string obj=4;
}